version '3'

services:

  redis:
    restart: always
    image: redis:latest
    ports:
      - "6379:6379"

  fastapi_app:
    container_name: fastapi_app_container
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
